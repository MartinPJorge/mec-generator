library(cluster)
library('SDMTools') # for Vicenty's distance

#' Performs the PAM clustering with k clusters
#' @param anntenasLoc matrix with two columns -latitude,longitude-
#' @param k number of clusters
#' @return TODO
pam_antennas <- function(antennasLoc, k) {
  # distances <- dist() <-- obtain distances according to Vicenty's formula
  
}
